# ??? DATABASE (LOCAL) ???
spring.datasource.url=jdbc:postgresql://localhost:5432/buildpos_db
spring.datasource.username=postgres
spring.datasource.password=postgres

# ??? DEV SOZLAMALAR ???
spring.jpa.show-sql=true
logging.level.com.buildpos=DEBUG

# ??? JWT ???
jwt.secret=buildpos-secret-key-2025-very-long-string-for-security
jwt.expiration=86400000

# Flyway — V6 tartibidan tashqari bajarish uchun (bir martalik)
spring.flyway.out-of-order=true

# CORS — lokal dev da barcha originlarga ruxsat
app.cors.allowed-origins=*